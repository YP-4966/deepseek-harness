[Unit]
Description=dsh rewrite proxy (loopback trust fence bypass for remote access)
After=network.target

[Service]
WorkingDirectory=/opt/dsh
ExecStart=/usr/bin/node rewrite-proxy.mjs
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
